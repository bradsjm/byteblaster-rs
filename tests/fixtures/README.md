# Fixtures

Place binary stream fixtures (`*.bin`) and expected outputs (`*.json`) here.

Current corpus index: `corpus.json`.

- `sync_recovery_with_garbage`: leading garbage + valid frame sync recovery.
- `malformed_header_rejected`: invalid data block header bytes.
- `server_list_full_format`: regular + sat server update payload.
